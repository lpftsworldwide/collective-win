# ============================================
# COLLECTIVE-WINS ENVIRONMENT VARIABLES
# ============================================
# 
# Only 4 variables are actually required!
# Everything else is optional or unused.
#
# ============================================

# ────────────────────────────────────────────
# ✅ REQUIRED: Frontend (Vercel)
# ────────────────────────────────────────────
# These are used in: src/integrations/supabase/client.ts
# Set these in Vercel Dashboard → Settings → Environment Variables

VITE_SUPABASE_URL=https://yiorietrtfosjnpzznnr.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here

# ────────────────────────────────────────────
# ✅ REQUIRED: Backend (Supabase Edge Functions)
# ────────────────────────────────────────────
# These are used in: supabase/functions/spin/index.ts
# Set these in Supabase Dashboard → Project Settings → API
# (Edge Functions automatically have access to these)

SUPABASE_URL=https://yiorietrtfosjnpzznnr.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# ❌ REMOVED (Not Used):
# ============================================
# - WEB3_RPC_URL (not used in current codebase)
# - WALLET_PRIVATE_KEY (not used in current codebase)
# - SMTP_* (optional, Supabase handles emails by default)
# - VITE_GA_ID (optional analytics, not required)
# - Any external game provider APIs (you ARE the API!)
#
# ============================================
# 📋 NOTES:
# ============================================
# - Frontend vars must start with VITE_ (Vite requirement)
# - Backend vars are set in Supabase Dashboard, not .env
# - No external game provider APIs needed - you run your own!
# - All game logic is in YOUR Edge Functions
# - All game data is in YOUR Supabase database
#
# ============================================
